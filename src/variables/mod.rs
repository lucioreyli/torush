// use std::path::{Path, PathBuf};

// pub const TORUSH_WIN_ROOT: &str = "C:/Program Files/torush";
// static HOME: &String = &std::env::var("HOME").unwrap();
// pub static TORUSH_UNIX_ROOT: &Path = Path::new(&Path::new(HOME).join(".local/share/torush"));
